---
import MagneticButton from '../islands/MagneticButton';
import Footer from '../ui/Footer.astro';
import { copy } from '../../content/copy';
---

<section
  data-section="cta"
  class="min-h-screen flex flex-col items-center justify-center px-6 py-32"
  style="background: var(--nox-bg);"
>
  <div class="text-center max-w-3xl w-full">
    <!-- Main CTA headline -->
    <h2
      class="cta-headline text-display-lg mb-8"
      style="color: var(--nox-fg);"
    >
      {copy.cta.heading}
    </h2>

    <!-- Sub-headline -->
    <p
      class="cta-subhead text-body mb-16 max-w-md mx-auto"
      style="color: var(--nox-fg); opacity: 0.7;"
    >
      {copy.cta.subtext}
    </p>

    <!-- Magnetic button — React island -->
    <MagneticButton
      client:only="react"
      label={copy.cta.button}
      href="#"
    />

    <!-- Shipping note — small, muted, below button -->
    <p
      class="mt-8 text-label"
      style="color: var(--nox-fg); opacity: 0.35; text-transform: none; letter-spacing: 0.02em; font-weight: 400; font-size: 12px;"
    >
      {copy.cta.shipping}
    </p>
  </div>
</section>

<!-- Footer -->
<Footer />
