---
import { products } from '../../data/products';
import { useTranslations, isLocale } from '../../i18n/utils';
import ProductCard from '../ui/ProductCard.astro';

const locale = isLocale(Astro.currentLocale) ? Astro.currentLocale : 'es';
const t = useTranslations(locale);
---

<section
  data-lineup-section
  class="min-h-screen flex flex-col justify-center py-16 lg:py-0"
  style="background: var(--nox-bg); overflow: hidden;"
>
  <!-- Section heading -->
  <div class="px-8 lg:px-16 mb-12 lg:mb-16">
    <p
      class="text-label mb-4"
      style="color: var(--nox-accent);"
    >
      {t.lineup.subheading}
    </p>
    <h2
      class="text-display-lg"
      style="color: var(--nox-fg);"
    >
      {t.lineup.heading}
    </h2>
  </div>

  <!-- Card track — horizontal on desktop (GSAP), native scroll on mobile -->
  <div
    class="px-8 lg:px-16"
    style="overflow: hidden;"
  >
    <ul
      data-lineup-track
      class="flex gap-8 lg:gap-10"
      style="
        /* Mobile: native horizontal scroll */
        overflow-x: auto;
        scroll-snap-type: x mandatory;
        -webkit-overflow-scrolling: touch;
        scrollbar-width: none;
        padding-bottom: 16px;
      "
    >
      <style>
        [data-lineup-track]::-webkit-scrollbar {
          display: none;
        }

        @media (min-width: 1024px) {
          [data-lineup-track] {
            overflow: visible;
          }

          [data-product-card] {
            min-width: 280px;
          }
        }

        @media (max-width: 1023px) {
          [data-product-card] {
            min-width: 80vw;
          }
        }
      </style>

      {products.map((product) => (
        <ProductCard
          name={product.name}
          subtitle={product[`subtitle_${locale}` as 'subtitle_es' | 'subtitle_en']}
          notes={product[`notes_${locale}` as 'notes_es' | 'notes_en']}
          roast={product[`roast_${locale}` as 'roast_es' | 'roast_en']}
          price={product.price}
          time={product.time}
          imageUrl={product.imageUrl}
        />
      ))}
    </ul>
  </div>
</section>
